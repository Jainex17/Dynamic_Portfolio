import React, { useState } from "react";

interface Props {
  islogin: boolean;
}

const Profile: React.FC<Props> = ({islogin}) => {

  return (
    <>
        
    </>
  );
};

export default Profile;
